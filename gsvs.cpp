#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define IOS ios_base::sync_with_stdio(false); cin.tie(NULL);cout.tie(NULL)
int main()
{
	IOS;
	ll t;cin>>t;
    while(t--)
    {
       int n;cin>>n;
       int A[n],B[n];
       for(int i=0;i<n;i++)
       {
       	cin>>A[i];
        B[i]=A[i];
       }      
       sort(B,B+n)
       int k=n/2;
       int l=lic;
       for(int i=0;i<k;i++)
       {

       }
    }
return 0;
}
