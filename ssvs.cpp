#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define IOS ios_base::sync_with_stdio(false); cin.tie(NULL);cout.tie(NULL)
int main()
{
	IOS;
	ll t;cin>>t;
    while(t--)
    {
        int n;cin>>n;
        string s;cin>>s;
        string a="2020";
        dp[4][n+1];
        
        for (int i = 0; i < 4; ++i)
               {
               	for (int j = 0; j < n+1; ++j)
               	{
               		if()
               	}
               }       
    }
return 0;
}
