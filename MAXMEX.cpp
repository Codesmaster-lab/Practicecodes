#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define IOS ios_base::sync_with_stdio(false); cin.tie(NULL);cout.tie(NULL)
int main()
{
	IOS;
	ll t;cin>>t;
    while(t--)
    {
     ll n,m,s=1;cin>>n>>m;
     ll A[100000];
     std::vector<int> v;
     for (ll i = 0; i < n; ++i)
            {
            	cin>>A[i];
                
            }       

    }
return 0;
}
