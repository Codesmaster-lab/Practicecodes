#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define IOS ios_base::sync_with_stdio(false); cin.tie(NULL);cout.tie(NULL)
int main()
{
	IOS;
	ll t;cin>>t;
    while(t--)
    {
     int n,m,x,y;
    cin>>n>>m>>x>>y;
  // int k= max(((n-x)+(m-y)),max((abs(1-x)+(m-y)),max((abs(n-x)+(1-y)),(abs(1-x)+(1-y)))));
     
   cout<<1<<" "<<1<<" "<<n<<" "<<m<<endl;
     }
return 0;
}
