#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define IOS ios_base::sync_with_stdio(false); cin.tie(NULL);cout.tie(NULL)
#define INF INT_MAX

int check(string s,string t,int cnt)
{
	if(cnt==INT_MAX)
		{
			cnt=-1;
		return -1;
	     }
}

int main()
{
	IOS;
	ll t;cin>>t;
    while(t--)
    {
     string s,t;
     cin>>s;
     cin>>t;
     unordered_map
     int x=check(s,t,0);
     cout<<x<<endl; 
    }
return 0;
}
